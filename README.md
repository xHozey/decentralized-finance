#Local node run
```bash
// install dependencies
npm i
//run node
npx hardhat node
```

#Exercie1
#Deployement guide
to deploy the smartcontracts and also simulate the audit flow run this command
```bash
//to compile the smart contract
npx hardhat compile
//to deploy the smart contract
npx hardhat run scripts/exercise1.ts --network localhost
```
#Exercise2
to deploy the smartcontracts and also simulate the audit flow run this command
```bash
//to compile the smart contract
npx hardhat compile
//to deploy the smart contract
npx hardhat run scripts/exercise2.ts --network localhost
```

#Exercise3
to run tests run this command
```bash
npx hardhat test
```